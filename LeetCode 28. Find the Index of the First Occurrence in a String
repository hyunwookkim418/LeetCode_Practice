#C++

class Solution {
public:
    int strStr(string str1, string str2) {
        size_t found = str1.find(str2);
        if (found != string::npos)
        {
            return found;
        }
    return -1;
    }
};
